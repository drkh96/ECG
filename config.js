/* ============================================
   ECG SIMULATOR — GLOBAL CONFIGURATION FILE
   --------------------------------------------
   هذا الملف يحتوي جميع الإعدادات الطويلة الأمد
   التي يتحكم بها المستخدم أو النظام.
   يمكنك تعديل أي قيمة هنا بدون لمس باقي المشروع.
   ============================================ */

export const ECG_CONFIG = {

    /* -------------------------------
       السرعة الأفقية لحركة الورقة
       1 = بطيء، 2 = طبيعي، 3 = سريع
    ------------------------------- */
    speed: 2,

    /* -------------------------------
       Gain (الحجم العمودي للموجة)
       كلما زاد الرقم زادت سعة الإشارة
    ------------------------------- */
    gain: 40,

    /* -------------------------------
       أبعاد كل Lead
       يمكنك تعديل الحجم حسب رغبتك
    ------------------------------- */
    canvasWidth: 600,
    canvasHeight: 90,

    /* -------------------------------
       إعدادات ورقة ECG (Grid)
       تفعيل/إلغاء خطوط مربعات ECG
    ------------------------------- */
    showGrid: true,

    /* -------------------------------
       ألوان ECG
    ------------------------------- */
    colors: {
        background: "#0c111c",
        gridSmall: "rgba(255,0,0,0.08)",
        gridLarge: "rgba(255,0,0,0.20)",
        waveform: "#00ff90"
    }
};